% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimizer.R
\name{fsrs_anki_to_reviews}
\alias{fsrs_anki_to_reviews}
\title{Convert Anki Review Log to FSRS Format}
\usage{
fsrs_anki_to_reviews(revlog, min_reviews = 2)
}
\arguments{
\item{revlog}{A data.frame with Anki review data. Supports column names from
ankiR (\code{cid}, \code{ease}, \code{id}) or standard names
(\code{card_id}, \code{rating}, \code{time}).}

\item{min_reviews}{Minimum number of reviews required per card (default 2).}
}
\value{
A data.frame with columns: card_id, rating, delta_t
}
\description{
Converts an Anki review log (from ankiR or similar) to the format
required by \code{\link{fsrs_optimize}}.
}
\examples{
\dontrun{
library(ankiR)
revlog <- anki_revlog()
reviews <- fsrs_anki_to_reviews(revlog, min_reviews = 3)
result <- fsrs_optimize(reviews)
}
}
